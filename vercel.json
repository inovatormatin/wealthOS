{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/admin", "destination": "/admin/index.html" },
    { "source": "/admin/:path*", "destination": "/admin/index.html" }
  ],
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install && cd frontend && npm install && cd ../admin && npm install",
  "buildCommand": "cd frontend && npm run build && cd ../admin && npm run build && cp -r dist ../frontend/dist/admin"
}
